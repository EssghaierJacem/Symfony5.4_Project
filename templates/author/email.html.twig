<head>
    <meta charset="UTF-8">
    <title>List of Authors: Sorted By Mail</title>
    <style>
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }
        h5 {
            color: #ff0000;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table, th, td {
            border: 1px solid #ccc;
        }
        th, td {
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #333;
            color: #fff;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
        img {
            display: block;
            margin: 40px auto;
            border-radius: 70%;
        }
    </style>
    </head>
    <body>
            <h1>List of Authors</h1>
    <table>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Books Published</th>
                    <th>Action</th>
                </tr>
                {% for author in authors %}
                    <tr>
                        <td>{{ author.id }}</td>
                        <td>{{ author.username|capitalize }}</td>
                        <td>{{ author.email }}</td>
                        <td>{{ author.nbrBook }}</td>
                        <td><a href=#>Show Details</a></td>
                    </tr>
                {% endfor %}