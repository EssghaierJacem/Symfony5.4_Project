<head>
    <meta charset="UTF-8">
    <title>Author Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .author-card {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
            padding: 20px;
            text-align: center;
            max-width: 400px;
        }

        .author-image {
            border-radius: 50%;
            width: 150px;
            height: 150px;
            margin: 0 auto 20px;
            object-fit: cover;
            border: 2px solid #333;
        }

        h1 {
            color: #333;
            margin-bottom: 10px;
        }

        p {
            color: #777;
            margin: 5px 0;
        }

        strong {
            color: #333;
        }

        .author-details {
            margin-top: 20px;
            text-align: left;
        }

        .author-details h2 {
            font-size: 18px;
            margin-bottom: 10px;
            color: #333;
        }

        .author-details ul {
            list-style-type: none;
            padding: 0;
        }

        .author-details li {
            margin-bottom: 5px;
        }

        .author-link {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }

        .author-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="author-card">
        <img src="{{ asset(author.image) }}" alt="{{ author.username }} Image" class="author-image">
        <h1>{{ author.username }}</h1>
        <div class="author-details">
            <h2>Author Details:</h2>
            <ul>
                <li><strong>ID:</strong> {{ author.id }}</li>
                <li><strong>Email:</strong> {{ author.email }}</li>
                <li><strong>Books Published:</strong> {{ author.nb_books }}</li>
            </ul>
        </div>
        <p>Explore more about this author <a href="#" class="author-link">here</a>.</p>
    </div>
</body>
