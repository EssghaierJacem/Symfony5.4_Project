<head>
    <meta charset="UTF-8">
    <title>List of books by Authors</title>
    <style>
        h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }
        h5 {
            color: #ff0000;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        table, th, td {
            border: 1px solid #ccc;
        }
        th, td {
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #333;
            color: #fff;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
        img {
            display: block;
            margin: 40px auto;
            border-radius: 70%;
        }
    </style>
</head>
 <body>
         <h1>List of Books</h1>
         <table>
                          <tr>
                              <th>Réf</th>
                              <th>Title</th>
                              <th>Published</th>
                              <th>Publication Date</th>
                              <th>Category</th>
                              <th>Author</th>

                          </tr>
          {% for book in books %}
                             <tr>
                              <td>{{ book.ref }}</td>
                              <td>{{ book.title | upper }}</td>
                              <td>{{ book.enabled }}</td>
                              <td>{{ book.publicationDate | date }}</td>
                              <td>{{ book.category }}</td>
                              <td>{{ book.author }}</td>
                              </tr>
         {% endfor %}
                </table>